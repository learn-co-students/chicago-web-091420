<%= form_for(@post) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.label :user %>
  <%# <%= f.text_area :user %>
  <%= f.collection_select(:user_id, @users, :id, :name) %>

  <%= f.submit %>
<% end %>